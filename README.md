**🌟 Nexus Hex: The Idle Frontier – Conquer the Unknown, One Hex at a Time! 🌟**

**Join your crew and watch a wild world bloom from a single safe hex.**  
**Idle progression means your heroes grind while you chill – log in for epic loot drops.**  
**Coop shines: Guilds claim frontiers, raid bosses together, and defend against fog monsters.**  
**Research suggests idle games hook millions with "numbers go up" dopamine and offline gains, while coop adds that irreplaceable friend-bond thrill.**

### Why You'll Get Hooked (Solo or Squad)
Picture this: Your NFT heroes (or free ones – fully viable F2P!) venture into foggy hexes. They chop forests, mine ores, build towers – all async, 1-24hr tasks. Return to treasures, XP, and a map that's *grown*.  
**Fun factor?** Endless progression: Level chars independently, collect synergies for speed boosts, prestige for multipliers. It's relaxing yet strategic – like AFK Arena's casual grind but with shared conquests.

### The Coop Magic That'll Keep You Coming Back
No more solo slogs! Form guilds (50 max), claim regions, pool resources. **10 players mining? Yields explode!** Defend edges from waves – fail, and fog swallows your turf. Boss expeditions need 5+ heroes for shared glory.  
**It seems likely that this sparks real bonds:** Players rave about guild raids and leaderboards in idles, turning "me-time" into "we conquered that!" vibes. Shards prevent overcrowding – migrate, raid rivals!

**TL;DR: Low-stress idle + heartfelt coop = Your next addiction. Share this, rally your squad, and hex-plode into fun! 🚀**

---

### 🌍 Full Concept Deep Dive: Why Nexus Hex Feels Like Destiny's Raid Boss Met Your Favorite Clicker

Nexus Hex blends the hypnotic "just one more upgrade" pull of idle games with the electric "we did it!" rush of coop frontiers. Imagine a persistent hex world starting as a lone safe tile, expanding infinitely as YOU and your guildmates push back the fog. Heroes toil offline, but victories are *shared* – evoking that Stardew Valley farm-raise joy, but with monsters and megabases. Backed by player stories: Idles thrive on passive progress (dopamine loops from rising stats), while multiplayer amps retention via social hooks like joint raids.

#### Core Loop: Set It, Forget It, Feast Later
1. **Roster Up**: Unlimited chars (NFTs for flair/boosts, F2P earns 'em weekly). Each levels solo – assign all at once for parallel power!
2. **Map Gaze**: Procedural hex grid unfurls. Fog hides dangers; reveal via scouts.
3. **Task Blitz**: Drag heroes to tiles – Explore (uncover), Mine (loot), Defend (waves), Build (towers/shrines). Modes: Rushed (quick/risky), Focused (safe/big), Standard (chill).
4. **Reap & Repeat**: Offline accrues FULL rewards. Spend on upgrades; watch synergies shine (your squad on one task? +50% speed!).

| Task Type | Time | Why Addictive? | Coop Boost |
|-----------|------|----------------|------------|
| **Explore** | 2-6h | Reveals secrets – "Ooh, ruins!" | Group uncovers 6x tiles |
| **Mine/Gather** | 4-12h | Resources stack exponentially | +15%/player; guild pots overflow |
| **Defend** | 1-4h | Auto-waves hit edges | Shared duty – lose tile, all feel it! |
| **Build** | 8-24h | Towers auto-guard | Multi speeds 20%/hero |
| **Expedition** | 12-48h | Boss loot raids | 5+ chars; guild glory shares |
| **Craft/Research** | 6-18h | Consumables (+yield bombs) | Unlocks guild buffs |

Success? Gear/stats + synergies beat risks. Fails give XP – no total loss.

#### World of Wonder: Hexes That Live & Breathe
Infinite procedural biomes (Perlin magic) from central origin. Edges spawn horrors – undefended? Fog reclaim! Guilds *claim* zones for exclusive farms.

| Tile | Vibes & Tasks | Loot | Threats | Defense Need |
|------|---------------|------|---------|--------------|
| **Forest** | Chop/Scout | Wood, Herbs | Bugs | Low |
| **Mine** | Dig/Tunnel | Ore, Gems | Moles | Med |
| **Ruins** | Excavate | Artifacts | Ghosts | High |
| **Swamp** | Forage | Shrooms | Slimes | Med |
| **Plains** 🆕 | Farm/Patrol | Food | Bandits | Low |
| **Mountain** 🆕 | Quarry | Crystals | Slides | High |
| **Fortress** | Raid | Gear | Bosses | Epic |

Events: Fog storms, invasions – weekly chaos unites guilds!

#### Progression: Your Empire, Your Rules
- **Chars**: Per-hero XP/stats (Health/Speed/Yield). Prestige resets for 1.5x mult + cosmetics.
- **Roster Perks**: 10+ chars? Global +5% yields. Synergies: Owned team on task = stacked buffs.
- **Buildings**:

| Build | Cost | Power Move | Upgrades |
|-------|------|------------|----------|
| **Tower** | Wood/Ore | Auto-defends 3 hexes | Wave-crusher |
| **Shrine** | Rares | +10% guild yield | Shard-wide! |
| **Outpost** | Food/Stone | +Slots/Trade | Tax riches |
| **Portal** 🆕 | Relics | Shard-hop raids | Import loot |

Resources curve x1.15^level – satisfying grind.

#### Coop Heartbeat: Why It *Feels* Like Winning Together
Idles solo? Meh. Here, **shared persistence** creates stories: "Our guild pushed to Volcano Zone overnight!" Free-riders? Contribution scores gate rewards. Solos get homesteads, but guilds multiply fun – markets, chats, PvP raids later.  
Players love this: "Coop farming feels epic," per recent buzz. Shards (100+) scale – conquer, migrate!

#### NFTs? Optional Spice
Cosmetics/traits (+niche boosts). F2P matches via quests. Trade in-game – no P2W walls.

#### Events & Endgame: Keeps It Fresh
- Consumables: Lucky Picks (+yield).
- Globals: Double rewards weeks.
- Seasons: Reset with legacies.
- GvG: Raid rival shards!

**Why *This* Game Rules:** Idles addict via offline highs (AFK Arena vibes), but coop? That's the secret sauce – social progression skyrockets engagement (raids, leaderboards). Like Idle Hex Realms but multiplayer frontier-pushing. Built web-light (PHP/Vue) for instant play.

**Grab friends, claim your hex legacy – the frontier awaits! Who's in? 👥✨**

**Key Citations**
- [Unveiling Idle Games Allure](https://medium.com/@smartmoneygame/unveiling-the-allure-of-idle-games-how-they-work-and-why-theyre-so-addictive-4e6e4f015d7f)
- [Idle Games Guide](https://apptrove.com/a-guide-to-idle-games/)
- [Multiplayer in Idle Games Reddit](https://www.reddit.com/r/incremental_games/comments/x5hepx/multiplayer_in_idle_games/)
- [AFK Arena Worth Playing Reddit](https://www.reddit.com/r/afkarena/comments/1dl37kh/is_afk_arena_worth_play_in_2024/)
- [Idle Hex Realms](https://play.google.com/store/apps/details?id=hu.nagartworks.hex)
- [Coop Idle X Post](https://x.com/Razor_Language/status/1891521125336645824)
- [No Rest Co-op Excitement](https://x.com/thomasmahler/status/1966769207770587590)
- [Reinventing Idle Social](https://www.kolibrigames.com/blog/were-on-a-mission-to-reinvent-idle/)

---

## 🛠 Generic Loader Service & Overlay
A lightweight reactive service powers all loading overlays, enabling multiple concurrent loaders (e.g. world gen, asset prefetch, network sync).

### Loader API (`src/core/loader.ts`)
Fields: `id`, `title`, `status`, `progress` (0-1), `completed`, `total`, `unitLabel`, `active`.

Helpers:
- `createLoader(id, { title, status?, total?, completed?, unitLabel? })`
- `updateLoader(id, patch)` – Patch fields and auto-recompute progress.
- `finishLoader(id, finalStatus?)` – Marks complete + sets `active = false`.
- `getLoader(id)` – Access a loader directly.
- `getActiveLoaders()` – Computed array of active loaders for UI.

### Overlay Component (`src/components/LoadingOverlay.vue`)
Renders when at least one active loader exists. Each loader displays:
- Title
- Status message
- Progress bar + percentage
- Completed / Total units

Adds blur + dim background (`bg-slate-900/80 backdrop-blur-sm`). Multiple loaders stack vertically.

### World Generation Integration
`world.ts` now wraps its generation process in a loader with id `world-gen`. Legacy refs (`generationInProgress`, `generationStatus`, etc.) remain exported for backward compatibility.

### Adding Another Loader (Example)
```ts
import { createLoader, updateLoader, finishLoader } from '@/core/loader';

async function preloadAssets(list: string[]) {
  const loader = createLoader('asset-preload', { title: 'Preloading Assets', status: 'Starting...', unitLabel: 'Assets', total: list.length });
  for (let i = 0; i < list.length; i++) {
    updateLoader(loader.id, { status: `Loading ${list[i]}`, completed: i });
    await fetch(list[i]); // placeholder
  }
  finishLoader(loader.id, 'Assets ready');
}
```
The overlay will automatically show while assets are loading.

### Future Enhancements
- Auto purge inactive loaders after delay
- Error state (`failed: boolean`, `errorMessage`)
- Cancellable loaders with abort signal
- Sort by priority
- Compact mode vs detailed mode

---

## Menus / Game Phases
The UI phase system now supports non-pausing in-game menus:
- Phases: `title`, `playing`
- In-game menu overlay: `PauseMenu.vue` (will be renamed `InGameMenu.vue`) displays without halting simulation.

Escape toggles the menu (`uiStore.menuOpen`), but hero movement, task progression, and camera inertia continue. Input (drag/keys) is suppressed while open.

### Actions
From `store/uiStore`:
- `startNewGame()`
- `continueGame()`
- `pauseGame()` / `resumeGame()` (semantic: open/close menu)
- `toggleMenu()`
- `returnToTitle()`

## Real-Time Task Progression
Tasks have migrated from tick-based to elapsed time progression. Each `TaskInstance` now stores:
- `createdMs`, `lastUpdateMs`, `completedMs?` timestamps (Date.now based)
- Legacy `createdTick`/`completedTick` kept temporarily for transition
- Contributions scale by seconds: `heroRate()` is treated as XP per second.

On each frame `updateActiveTasks()` computes `elapsedMs` since `lastUpdateMs` and applies `ratePerSecond * (elapsedMs/1000)` per hero. Offline gaps are captured by restoring tasks and immediately synchronizing timestamps; a subsequent update after heroes load awards full catch-up.

### Balancing Notes
Previous required XP values assumed ~60 ticks/sec; they've been reduced for the `explore` task. Future tuning can adjust either `requiredXp()` or `heroRate()` for pacing.

### Persistence
Tasks serialize minimal fields under `driftlands_tasks_v2`. Heroes persist separately. On load, tasks restore then wait for a hero update pass to apply real progress.

### Edge Cases Handled
- Empty participant set removes task.
- Negative elapsed or zero elapsed are ignored.
- Multiple completions in same frame handled by array slice iteration.
- Offline gap sets baseline timestamp to avoid partial double-counting until heroes restored.

---

## 🌱 Timed Terrain Variant Growth
Certain terrain variants naturally age over real time. This enables ecological recovery flows (e.g. replanting chopped forests) without manual tasks beyond the initial action.

### How It Works
- Variants can declare a `growth` config in `src/core/terrainDefs.ts`:
  - `growth: { next: string | null; ageMs: number }`
  - `next: null` means the tile returns to its base terrain appearance (variant cleared).
  - If `next` points to another variant that itself has `growth`, the chain continues.
- When a variant with growth is applied, the tile records `variantSetMs = Date.now()` and is added to the aging tracker.
- Each frame (`idleStore` loop) `updateTileGrowth()` checks aging tiles and transitions those whose elapsed time >= `ageMs`.
- World redraw is triggered via `worldVersion` bumps.

### Current Chain
- `forest` variant `young_forest` → (after 120000 ms) `null` (base forest art).

### Adding New Growth Stages
1. Add or modify a variant entry in its terrain definition:
```ts
{ key: 'sapling_forest', weight: 1, growth: { next: 'young_forest', ageMs: 60000 } }
{ key: 'young_forest', weight: 2, growth: { next: null, ageMs: 120000 } }
```
2. Ensure any code that manually sets a variant also updates:
```ts
 tile.variant = 'young_forest';
 tile.variantSetMs = Date.now();
 registerAgingTile(tile);
```
(You can alternatively create a helper if many call sites emerge.)

### Persistence
- `variantSetMs` is stored with each tile via existing world save serialization. On load we re-register aging tiles; matured variants will advance automatically after the required time passes.

### Edge Handling
- If a tile's variant changes early (e.g. by another task), its growth entry is removed automatically.
- Missing `variantSetMs` or removed growth definitions are cleaned during update passes.

### Performance
- Only tiles with active aging are tracked in a `Set`; per-frame work is proportional to aging tile count.

### Biome Scaling
Variants can define `biomeScale` multipliers inside their `growth` config. The effective maturation time = `ageMs * (biomeScale[biome] ?? 1)`.
Example (`young_forest`): faster in `forest` biome (0.8x), slower in harsh biomes like `dessert` (1.5x) or `snow` (1.4x).
